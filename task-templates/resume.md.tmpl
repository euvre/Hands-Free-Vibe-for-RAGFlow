Previous run: status={prev_status} exit={prev_exit}. First grep the previous run log (a cline --json event stream, possibly several MB — grep/tail in fragments ONLY, never read it whole) for key tool names/errors to determine how far it got, then continue from the interruption point; do not redo completed steps.
